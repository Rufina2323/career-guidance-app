# Career Prediction Service - MLPO Project

## Описание проекта
Web-приложение для прогнозирования профессий на основе навыков, интересов и академических данных студентов.  
Пользователи могут загружать данные, получать предсказания, просматривать историю запросов и баланс кредитов. Администратор может пополнять баланс пользователей и просматривать все транзакции.

---

## 🔹 Возможности

- **Прогноз карьеры**: Подбирает подходящие карьерные направления на основе технических и мягких навыков, а также интересов.  
- **История ML-запросов**: Просмотр прошлых запросов на прогноз и их результатов.  
- **Баланс и транзакции**: Отслеживание кредитов для ML-запросов и управление депозитами.  
- **Админ-панель** (для администраторов): Утверждение/отклонение депозитов и просмотр транзакций пользователей (⚠️ Регистрация администраторов через интерфейс невозможна — они добавляются напрямую в базу данных для обеспечения безопасности).

---

## 🔹 Как это работает

1. Заполните подробную анкету о навыках, образовании и интересах.  
2. Выберите ML-модель для прогнозирования карьеры.  
3. Отправьте запрос — система ставит его в очередь и обрабатывает с помощью **FastAPI** и **RabbitMQ**.  
4. Просматривайте результаты в разделе **История ML-запросов**.  
5. Ваши данные безопасно хранятся и используются только для прогнозирования.

---

## 🔹 Используемые технологии

- **Backend**: FastAPI  
- **Machine Learning**: ML-модели для прогнозирования карьеры  
- **Очередь задач**: RabbitMQ для асинхронной обработки ML-запросов  
- **Frontend**: Streamlit для интерактивной панели  
- **База данных**: PostgreSQL  
- **Аутентификация и безопасность**: Токен-базированные сессии пользователей  

> ⚠️ Для работы моделей необходимо добавить в базу пути к файлам моделей:  
> - `ml_models/career_prediction_model/mlp_model.pt`  
> - `ml_models/career_prediction_model/preprocessor.joblib`  
> - `ml_models/career_prediction_model/label_encoder.joblib`  

---

## 🔹 Примечания

- Прогнозы ML являются **только рекомендацией** и не гарантируют карьерных результатов.  
- Для точных прогнозов рекомендуется заполнять все поля анкеты максимально корректно.  
- Администраторы имеют дополнительные возможности, включая управление депозитами и просмотр активности пользователей.  

---

## 🔹 Как начать

1. Клонируйте репозиторий:

```bash
git clone https://github.com/Rufina2323/career-guidance-app.git
cd career-guidance-app
```

2. Запустите проект с помощью Docker Compose:
```bash
docker compose up --build
```
3. Для масштабирования воркеров (например, 5 параллельных воркеров):
```bash
docker compose up --scale worker=5
```

4. Используйте боковую панель Streamlit для навигации:
- **Баланс**: Управление счетом и депозитами
- **ML Request**: Отправка новых запросов на прогноз карьеры
- **ML History**: Просмотр прошлых прогнозов
- **Transactions**: Отслеживание транзакций